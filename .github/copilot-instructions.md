# LaunchKit — GitHub Copilot / VS Code adapter

Pointer only. Follow these repo files instead of inventing a parallel architecture:

- `AI_RULES.md` — how to change the codebase
- `PROJECT_CONTEXT.md` — product, stack, decisions
- `ARCHITECTURE.md` — structure and data flow
- `MODULES.md` — what to reuse

Setup (env, SQL): `DOCUMENTATION.md`. Roadmap: `PLAN.md`.

Inspect existing `src/` code before adding files. Reuse `src/config`, `src/content`, `src/components/ui`, and `src/lib/*`.
